# Put your TODOs for this branch here, then make sure it is empty before
# landing. There is a test to ensure it is empty in trunk.

 * trigger the onchange of the date when the date is set, rather than calling
   the dateSelected method
 * show the start and end sprint times in date widget by default
 * allow registration from start date less one day,
 * figure out why the date shows seconds sometimes 
