The series-targeted portlet displays the number of open bugs that have
been accepted as targeting a specific series of a distribution:

  >>> anon_browser.open("http://bugs.launchpad.dev/debian/+bugs")
  >>> portlet = find_portlet(anon_browser.contents, "Series-targeted bugs")
  >>> print extract_text(portlet)
  Series-targeted bugs
  sid   0
  sarge 1
  woody 2

  >>> anon_browser.open("http://bugs.launchpad.dev/debian/sarge/+bugs")
  >>> portlet = find_portlet(anon_browser.contents, "Series-targeted bugs")
  >>> print extract_text(portlet)
  Series-targeted bugs
  sid   0
  sarge 1
  woody 2

  >>> anon_browser.open("http://bugs.launchpad.dev/ubuntu/+bugs")
  >>> portlet = find_portlet(anon_browser.contents, "Series-targeted bugs")
  >>> print extract_text(portlet)
  Series-targeted bugs
  breezy-autotest 0
  grumpy          0
  hoary           1
  warty           1

  >>> print anon_browser.getLink("breezy-autotest").url
  http://bugs.launchpad.dev/ubuntu/breezy-autotest/+bugs

The same portlet is available on the bugs homepage for the distro:

  >>> anon_browser.open("http://bugs.launchpad.dev/debian/+bugs-index")
  >>> portlet = find_portlet(anon_browser.contents, "Series-targeted bugs")
  >>> print extract_text(portlet)
  Series-targeted bugs
  sid   0
  sarge 1
  woody 2

  >>> anon_browser.open("http://bugs.launchpad.dev/debian/sarge/+bugs-index")
  >>> portlet = find_portlet(anon_browser.contents, "Series-targeted bugs")
  >>> print extract_text(portlet)
  Series-targeted bugs
  sid   0
  sarge 1
  woody 2

It's also available for project and project series listings and
homepages:

  >>> anon_browser.open("http://bugs.launchpad.dev/firefox/+bugs")
  >>> portlet = find_portlet(anon_browser.contents, "Series-targeted bugs")
  >>> print extract_text(portlet)
  Series-targeted bugs
  1.0   1
  trunk 0

  >>> anon_browser.open("http://bugs.launchpad.dev/firefox/1.0/+bugs")
  >>> portlet = find_portlet(anon_browser.contents, "Series-targeted bugs")
  >>> print extract_text(portlet)
  Series-targeted bugs
  1.0   1
  trunk 0

  >>> anon_browser.open("http://bugs.launchpad.dev/firefox/+bugs-index")
  >>> portlet = find_portlet(anon_browser.contents, "Series-targeted bugs")
  >>> print extract_text(portlet)
  Series-targeted bugs
  1.0   1
  trunk 0

  >>> anon_browser.open("http://bugs.launchpad.dev/firefox/1.0/+bugs-index")
  >>> portlet = find_portlet(anon_browser.contents, "Series-targeted bugs")
  >>> print extract_text(portlet)
  Series-targeted bugs
  1.0   1
  trunk 0

