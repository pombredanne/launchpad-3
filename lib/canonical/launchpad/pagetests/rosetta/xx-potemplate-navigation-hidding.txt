There are some different conditions that produces that we hide POTemplate
objects from the navigation for non administrator users.

 - When a project doesn't has the 'Use Translations' flag set:

    For this example, we will use netapplet project. It doesn't has such flag
    set.

    >>> browser.open('http://launchpad.dev/netapplet')
    >>> for tag in find_tags_by_class(browser.contents, 'summary'):
    ...     print extract_text(tag.findNext('ul').renderContents())
    Active
    Doesn&rsquo;t use Bugs
    Doesn&rsquo;t use Translations
    Doesn&rsquo;t use Answers
