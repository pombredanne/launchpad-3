We should be able to edit the details on a sprint but the menus are only
available to those who have permissions to edit that sprint.

  >>> anon_browser.open('http://launchpad.dev/sprints/ubz')
  >>> anon_browser.title
  'Ubuntu Below Zero (sprint or meeting)'

  >>> anon_browser.getLink('Edit Details')
  Traceback (most recent call last):
  ...
  LinkNotFoundError

We will log in as Sample Person and edit the ubz sprint.

  >>> user_browser.open('http://launchpad.dev/sprints/ubz')
  >>> user_browser.title
  'Ubuntu Below Zero (sprint or meeting)'

  >>> address = 'Holiday Inn Select, Downtown Montreal, Canada'
  >>> address in user_browser.contents
  False
  >>> user_browser.getLink('Edit Details').click()
  >>> user_browser.url
  'http://launchpad.dev/sprints/ubz/+edit'

If we alter the date to an ending date that precedes the starting date we 
should receive a nice error message.

  >>> user_browser.getControl(
  ...     'Starting Date and Time').value = '2006-01-10 23:30'
  >>> user_browser.getControl(
  ...     'Finishing Date and Time').value = '2005-02-12 00:11'
  >>> user_browser.getControl('Continue').click()

  >>> "This event can't start after it ends" in user_browser.contents
  True

We fix the dates and change the address, we expect to be redirected to the
sprint home page. The time displayed will be adjusted to Sample Person's time
zone (Australia/Perth UTC+8).

  >>> user_browser.getControl('Timezone').value = ['America/Montreal']
  >>> user_browser.getControl(
  ...     'Starting Date and Time').value = '2006-01-10 08:30'
  >>> user_browser.getControl(
  ...     'Finishing Date and Time').value = '2006-02-12 17:00'
  >>> user_browser.getControl('Meeting Address').value = address
  >>> user_browser.getControl('Continue').click()

  >>> user_browser.url
  'http://launchpad.dev/sprints/ubz'

  >>> address in user_browser.contents
  True

  >>> '2006-01-10 21:30:00 WST' in user_browser.contents
  True
  >>> '2006-02-13 06:00:00 WST' in user_browser.contents
  True
