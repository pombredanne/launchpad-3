Each product in Launchpad has a Translations page.

    >>> browser.open('http://launchpad.dev/products/evolution/+translations')
    >>> browser.url
    'http://launchpad.dev/products/evolution/+translations'
    >>> print browser.contents
    <...
    ...Translations of The Evolution Groupware Application...
    ...
    ...For Evolution, Launchpad currently recommends...
    ...Hoary...
    ...

If a product is not set up for translations in Rosetta, and you are its
registrant or an admin, the Translations page tells you how to proceed.

    >>> from zope.testbrowser.testing import Browser

    >>> privileged_browser = Browser()
    >>> privileged_browser.addHeader('Authorization', 'Basic mark@hbd.com:test')
    >>> privileged_browser.open(
    ...     'http://launchpad.dev/products/alsa-utils/+translations')
    >>> print privileged_browser.contents
    <...
    ...How to set up translations...
    ...
    .../products/alsa-utils/trunk/+translations-upload"...
    ...

If you're logged in as someone else, the Translations page suggests that you
contact the product's registrant, or look for a package that you can translate.

    >>> unprivileged_browser = Browser()
    >>> unprivileged_browser.addHeader(
    ...     'Authorization', 'Basic no-priv@canonical.com:test')
    >>> unprivileged_browser.open(
    ...     'http://launchpad.dev/products/alsa-utils/+translations')
    >>> print unprivileged_browser.contents
    <...
    ...talk with...Mark Shuttleworth...
    ...
    .../products/alsa-utils/+packages"...
    ...

And if you're not logged in at all, you might or might not be the registrant,
so the text reflects both possibilities.

    >>> anon_browser.open('http://launchpad.dev/products/alsa-utils/+translations')
    >>> print anon_browser.contents
    <...
    ...If you&#8217;re Mark Shuttleworth,...
    .../products/alsa-utils/+translations/+login"...log in...
    ...
    ...Otherwise...
    ...talk with...Mark Shuttleworth...
    ...
    .../products/alsa-utils/+packages"...
    ...
