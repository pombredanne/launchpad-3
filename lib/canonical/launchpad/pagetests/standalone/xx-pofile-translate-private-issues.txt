Messages which are available in our PO files to credit translators
should not be available for translation on the Launchpad page.

  >>> browser.open(
  ...     'http://launchpad.dev/alsa-utils/trunk/+pots/alsa-utils/es/'
  ...     '+translate')
  >>> print browser.url
  http://launchpad.dev/alsa-utils/trunk/+pots/alsa-utils/es/+translate

The GNOME standard string for credits is not displayed.

  >>> from BeautifulSoup import BeautifulSoup
  >>> msgid = find_tag_by_id(browser.contents, 'msgset_199_singular')
  >>> print msgid
  None


And the same for KDE one: it's not shown on the page at all.

  >>> msgid = find_tag_by_id(browser.contents, 'msgset_200_singular')
  >>> print msgid
  None
