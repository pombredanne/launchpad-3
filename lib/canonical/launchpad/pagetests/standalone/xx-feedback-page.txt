Launchpad has a feedback page, that is linked to from the bottom of
every other page on the site.

    >>> browser.open('http://launchpad.dev')
    >>> browser.getLink(url='/feedback').click()
    >>> browser.url
    'http://launchpad.dev/feedback'

    >>> browser.title
    'Help improve Launchpad'
