= Bugs a given user is involved with =

When visiting the 'Bugs' facet of a person, the report that is displayed
lists the bug assigned to that person.

    >>> anon_browser.open('http://launchpad.dev/~name12')
    >>> anon_browser.getLink('Bugs').click()
    >>> print anon_browser.title
    Bugs related to Sample Person
    >>> print anon_browser.url
    http://bugs.launchpad.dev/~name12/

Note that we may see each bug more than once in case it's reported against
more than one target (https://launchpad.net/bugs/1357).

    >>> from canonical.launchpad.ftests.bug import print_bugtasks
    >>> print_bugtasks(anon_browser.contents)
    5 Firefox install instructions should be complete
        firefox (upstream) Critical Unconfirmed
    4 Reflow problems with complex page layouts
        firefox (upstream) Medium Unconfirmed
    5 Firefox install instructions should be complete
        mozilla-firefox (Ubuntu Warty) Medium Unconfirmed
    1 Firefox does not support SVG
        mozilla-firefox (Ubuntu) Medium Unconfirmed
    2 Blackhole Trash folder
        mozilla-firefox (Debian Woody) Medium Unconfirmed
    9 Thunderbird crashes
        thunderbird (Ubuntu) Medium Confirmed
    1 Firefox does not support SVG
        firefox (upstream) Low Unconfirmed
    2 Blackhole Trash folder
        ubuntu (upstream) Low Unconfirmed
    1 Firefox does not support SVG
        mozilla-firefox (Debian) Low Confirmed
    2 Blackhole Trash folder
        mozilla-firefox (Debian) Low Confirmed

This report is also the default page in the person context on the bugs
virtual host:

    >>> anon_browser.open('http://bugs.launchpad.dev/~name12')
    >>> print anon_browser.title
    Bugs related to Sample Person

== More specific listings ==

On a person bugs facet we get links to all listings of bugs we have for that
person.

=== Assigned bugs ===

    >>> anon_browser.getLink('Assigned').click()
    >>> print anon_browser.title
    Bugs assigned to Sample Person
    >>> print anon_browser.url
    http://bugs.launchpad.dev/~name12/+assignedbugs

    >>> print_bugtasks(anon_browser.contents)
    5 Firefox install instructions should be complete
        firefox (upstream) Critical Unconfirmed
    2 Blackhole Trash folder
        mozilla-firefox (Debian) Low Confirmed

=== Reported bugs ===

    >>> anon_browser.getLink('Reported').click()
    >>> print anon_browser.title
    Bugs reported by Sample Person
    >>> print anon_browser.url
    http://bugs.launchpad.dev/~name12/+reportedbugs

    >>> print_bugtasks(anon_browser.contents)
    5 Firefox install instructions should be complete
        firefox (upstream) Critical Unconfirmed
    4 Reflow problems with complex page layouts
        firefox (upstream) Medium Unconfirmed
    5 Firefox install instructions should be complete
        mozilla-firefox (Ubuntu Warty) Medium Unconfirmed
    2 Blackhole Trash folder
        mozilla-firefox (Debian Woody) Medium Unconfirmed
    1 Firefox does not support SVG
        firefox (upstream) Low Unconfirmed
    2 Blackhole Trash folder
        ubuntu (upstream) Low Unconfirmed
    1 Firefox does not support SVG
        mozilla-firefox (Debian) Low Confirmed
    2 Blackhole Trash folder
        mozilla-firefox (Debian) Low Confirmed

=== Subscribed bugs ===

    >>> anon_browser.getLink('Subscribed').click()
    >>> print anon_browser.title
    Bugs Sample Person is subscribed to
    >>> print anon_browser.url
    http://bugs.launchpad.dev/~name12/+subscribedbugs

    >>> print_bugtasks(anon_browser.contents)
    4 Reflow problems with complex page layouts
        firefox (upstream) Medium Unconfirmed
    1 Firefox does not support SVG
        mozilla-firefox (Ubuntu) Medium Unconfirmed
    9 Thunderbird crashes
        thunderbird (Ubuntu) Medium Confirmed
    1 Firefox does not support SVG
        firefox (upstream) Low Unconfirmed
    1 Firefox does not support SVG
        mozilla-firefox (Debian) Low Confirmed

