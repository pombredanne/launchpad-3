= Removing an email =

'foo' doesn't want to use baz.foo@canonical.com on Launchpad. He removes it.

    >>> browser = setupBrowser(auth='Basic foo@baz.com:f')
    >>> browser.open('http://launchpad.dev/~foo/+editemails')
    >>> browser.getControl(name='field.VALIDATED_SELECTED').getControl(
    ...     value='baz.foo@canonical.com').selected = True
    >>> browser.getControl('Remove').click()
    >>> for msg in get_feedback_messages(browser.contents):
    ...     print msg
    The email address...has been removed...

    >>> browser.getControl(name='field.VALIDATED_SELECTED').getControl(
    ...     value='baz.foo@canonical.com')
    Traceback (most recent call last):
    ...
    LookupError: value 'baz.foo@canonical.com'
