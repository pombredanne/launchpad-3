= Appointing a Project Driver =

Appoint Sample Person as the driver of the GNOME Project.

  >>> browser.addHeader('Authorization', 'Basic test@canonical.com:test')
  >>> browser.open('http://launchpad.dev/gnome')
  >>> browser.getLink('Appoint driver').click()

  >>> browser.url
  'http://launchpad.dev/gnome/+driver'
  >>> browser.title
  'Appoint the driver for The GNOME Project'

  >>> for tag in find_tags_by_class(browser.contents, 'formHelp'):
  ...     print tag
  <p class="formHelp">This is a project-wide appointment, think carefully 
  here! This person or team will be able to set feature goals and approve 
  bug targeting and backporting for ANY series in ANY product in this project.
  You can also appoint drivers at the level of a specific product or series.
  So you may just want to leave this space blank, and instead let the 
  individual products and series have drivers...

  >>> browser.getControl('Driver').value = 'name12'
  >>> browser.getControl('Change').click()

After changing the driver, the user is redirected to the overview page where a 
message inform him of the driver change.

  >>> browser.url
  'http://launchpad.dev/gnome'

  >>> for tag in find_tags_by_class(browser.contents, 'informational'):
  ...     tag.renderContents()
  'Successfully changed the driver to test@canonical.com'

Sample Person is listed as the driver of the project.

  >>> project_portlet = find_portlet(browser.contents, 'The GNOME Project')
  >>> project_portlet.find(text='Driver:').findNext('a').string
  u'Sample Person'
