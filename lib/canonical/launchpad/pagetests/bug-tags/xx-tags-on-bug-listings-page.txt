On the bug listings page there is a portlet which shows all tags that
have been used for bugs in this context.

    >>> anon_browser.open('http://launchpad.dev/ubuntu/+bugs')
    >>> anon_browser.url
    'http://launchpad.dev/ubuntu/+bugs'
    >>> tags_portlet = find_tag_by_id(anon_browser.contents, 'portlet-tags')
    >>> for td_tag in tags_portlet('td'):
    ...     print td_tag.renderContents()
    <a ...>crash</a>
    2
    <a ...>dataloss</a>
    1
    <a ...>pebcak</a>
    1

If we click on a tag, only bugs with that tag are displayed:

    >>> anon_browser.getLink(url='crash').click()
    >>> anon_browser.url
    'http://launchpad.dev/ubuntu/+bugs?field.tag=crash'

    >>> from canonical.launchpad.ftests.bug import print_bugtasks
    >>> print_bugtasks(anon_browser.contents)
    9 Thunderbird crashes
      thunderbird Medium Confirmed
    10 another test bug
       linux-source-2.6.15 Medium New

Clicking on a tags shows only bugs that have that specific tag, so if
we click on another tag, the bugs that were shown previously won't be
shown.

    >>> anon_browser.getLink('dataloss').click()
    >>> anon_browser.url
    'http://launchpad.dev/ubuntu/+bugs?field.tag=dataloss'
    >>> print_bugtasks(anon_browser.contents)
    2 Blackhole Trash folder
      &mdash; Medium New

The links don't work as a filter to the existing listing, it's a filter
of the default listing. So if we would show only the invalid bugs, and
then press the tag link again, only open bugs will be shown

    >>> anon_browser.open(
    ...     'http://launchpad.dev/ubuntu/+bugs?advanced=1')
    >>> anon_browser.getControl(name='field.status:list').displayValue = ['Invalid']
    >>> anon_browser.getControl('Search', index=0).click()
    >>> print_bugtasks(anon_browser.contents)

    >>> anon_browser.getLink('dataloss').click()
    >>> anon_browser.url
    'http://launchpad.dev/ubuntu/+bugs?field.tag=dataloss'
    >>> print_bugtasks(anon_browser.contents)
    2 Blackhole Trash folder
      &mdash; Medium New
