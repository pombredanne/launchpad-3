At first there should be no translators in the polyglot group.

  >>> browser.addHeader('Authorization', 'Basic carlos@canonical.com:test')
  >>> browser.open(
  ...   'http://translations.launchpad.dev/translations/groups/polyglot/')
  >>> for tag in find_tags_by_class(browser.contents, 'message'):
  ...     print tag.renderContents()
  <BLANKLINE>
  There are no translators appointed in this group yet.
  <BLANKLINE>

Now, let's appoint a translator. First see if we can display the
appointments page.

  >>> browser.getLink('Appoint').click()
  >>> browser.url
  'http://translations.launchpad.dev/translations/groups/polyglot/+appoint'

Now let's make an appointment. Hoary Gnome Team will translate into
Abkhasian. We should get redirected back to the group page.

  >>> browser.getControl('Language').value=['ab']
  >>> browser.getControl('Translator').value='name21'
  >>> browser.getControl('Add').click()

And let's appoint No Privileges user for Afrikaans too.

  >>> browser.getControl('Language').value=['af']
  >>> browser.getControl('Translator').value='no-priv'
  >>> browser.getControl('Add').click()

Now we should see both of those appointments on the polyglot page:

  >>> print http(r"""
  ... GET /translations/groups/polyglot/ HTTP/1.1
  ... Authorization: Basic Zm9vLmJhckBjYW5vbmljYWwuY29tOnRlc3Q=
  ... """)
  HTTP/1.1 200 Ok
  ...The PolyGlot Translation Group...
  ...
        <td>Abkhazian</td>
        <td><a href=".../~name21">Hoary Gnome Team</a>
  ...
        <td>Afrikaans</td>
        <td><a href=".../~no-priv">No Privileges Person</a>
  ...
  ...href="+appoint"...Appoint...

