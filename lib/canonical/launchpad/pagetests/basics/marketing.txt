= Launchpad Marketing Microsite =

From Launchpad's front page, you can access a "marketing microsite"

    >>> browser.open('http://launchpad.dev/')
    >>> browser.getLink('What is Launchpad?').click()
    >>> print browser.title
    About Launchpad
    >>> browser.url
    'http://launchpad.dev/+about'

That features three large tabs: "Home", "Get an account", and
"Take a tour".

    >>> navigation = find_tag_by_id(browser.contents, 'navigation')
    >>> home_link = navigation.find('a')
    >>> print home_link.renderContents()
    Home
    >>> print home_link['href']
    http://launchpad.dev/+about

    >>> account_link = browser.getLink('Get an account')
    >>> account_link.url
    'http://launchpad.dev/+about/+login'

    >>> tour_link = browser.getLink('Take a tour')
    >>> tour_link.url
    'http://launchpad.dev/+tour'

== About each application ==

The 'About Launchpad' page has one link to each Launchpad application
which brings the Overview page for that application. The overview page
has a link to the FAQs and Tour page for the application.

=== Code ===

    >>> browser.open('http://launchpad.dev/+about')
    >>> browser.getLink('Code').click()
    >>> print browser.title
    About Code
    >>> print browser.url
    http://code.launchpad.dev/+about

    >>> browser.getLink('Application tour').click()
    >>> print browser.title
    Code tour:...
    >>> print browser.url
    http://code.launchpad.dev/+tour

    >>> browser.getLink('FAQs').click()
    >>> print browser.title
    FAQs about Code
    >>> print browser.url
    http://code.launchpad.dev/+faq

=== Bugs ===

    >>> browser.open('http://launchpad.dev/+about')
    >>> browser.getLink('Bugs').click()
    >>> print browser.title
    About Bugs
    >>> print browser.url
    http://bugs.launchpad.dev/+about

    >>> browser.getLink('Application tour').click()
    >>> print browser.title
    Bugs tour:...
    >>> print browser.url
    http://bugs.launchpad.dev/+tour

    >>> browser.getLink('FAQs').click()
    >>> print browser.title
    FAQs about Bugs
    >>> print browser.url
    http://bugs.launchpad.dev/+faq

=== Blueprints ===

    >>> browser.open('http://launchpad.dev/+about')
    >>> browser.getLink('Blueprints').click()
    >>> print browser.title
    About Blueprints
    >>> print browser.url
    http://blueprints.launchpad.dev/+about

    >>> browser.getLink('Application tour').click()
    >>> print browser.title
    Blueprints tour:...
    >>> print browser.url
    http://blueprints.launchpad.dev/+tour

    >>> browser.getLink('FAQs').click()
    >>> print browser.title
    FAQs about Blueprints
    >>> print browser.url
    http://blueprints.launchpad.dev/+faq

=== Translations ===

    >>> browser.open('http://launchpad.dev/+about')
    >>> browser.getLink('Translations').click()
    >>> print browser.title
    About Translations
    >>> print browser.url
    http://translations.launchpad.dev/+about

    >>> browser.getLink('Application tour').click()
    >>> print browser.title
    Translations tour:...
    >>> print browser.url
    http://translations.launchpad.dev/+tour

    >>> browser.getLink('FAQs').click()
    >>> print browser.title
    FAQs about Translations
    >>> print browser.url
    http://translations.launchpad.dev/+faq

=== Answers ===

    >>> browser.open('http://launchpad.dev/+about')
    >>> browser.getLink('Answers').click()
    >>> print browser.title
    About Answers
    >>> print browser.url
    http://answers.launchpad.dev/+about

    >>> browser.getLink('Application tour').click()
    >>> print browser.title
    Answers tour:...
    >>> print browser.url
    http://answers.launchpad.dev/+tour

    >>> browser.getLink('FAQs').click()
    >>> print browser.title
    FAQs about Answers
    >>> print browser.url
    http://answers.launchpad.dev/+faq

== Link to tour on each application main page ==

Each application home page features a proeminent 'Take a tour'
button which brings the user to the application tour.

    >>> browser.open('http://code.launchpad.dev')
    >>> browser.getLink('Take a tour').click()
    >>> print browser.title
    Code tour:...

    >>> browser.open('http://bugs.launchpad.dev')
    >>> browser.getLink('Take a tour').click()
    >>> print browser.title
    Bugs tour:...

    >>> browser.open('http://blueprints.launchpad.dev')
    >>> browser.getLink('Take a tour').click()
    >>> print browser.title
    Blueprints tour:...

    >>> browser.open('http://translations.launchpad.dev')
    >>> browser.getLink('Take a tour').click()
    >>> print browser.title
    Translations tour:...

    >>> browser.open('http://answers.launchpad.dev')
    >>> browser.getLink('Take a tour').click()
    >>> print browser.title
    Answers tour:...

