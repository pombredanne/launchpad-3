Each of the +bugs bug listings have a sort order widget. Let's
demonstrate this functionality on each +bugs page.

1. The upstream +bugs page.

Default view:

    >>> browser.open("http://localhost:9000/firefox/+bugs")

    >>> print browser.contents
    <!DOCTYPE...
    ...Firefox install instructions should be complete...
    ...Reflow problems with complex page layouts...
    ...Firefox does not support SVG...

Newest:

    >>> browser.getControl(name="orderby").displayValue = ["newest"]
    >>> browser.getControl("Search").click()

    >>> print browser.contents
    <!DOCTYPE...
    ...Firefox install instructions should be complete...
    ...Reflow problems with complex page layouts...
    ...Firefox does not support SVG...

2. The distro +bugs page.

Default view:

    >>> browser.open("http://localhost:9000/debian/+bugs")
    >>> print browser.contents
    <!DOCTYPE...
    ...Bug Title Test...
    ...Firefox does not support SVG...
    ...Blackhole Trash folder...

By recently changed:

    >>> browser.getControl(name="orderby").displayValue = ["recently changed"]
    >>> browser.getControl("Search").click()
    >>> print browser.contents
    <!DOCTYPE...
    ...Bug Title Test...
    ...Blackhole Trash folder...
    ...Firefox does not support SVG...

By status:

    >>> browser.open("http://localhost:9000/ubuntu/+bugs")
    >>> browser.getControl(name="orderby").displayValue = ["by status"]
    >>> browser.getControl("Search").click()
    >>> print browser.contents
    <!DOCTYPE...
    ...Firefox does not support SVG...
    ...another test bug...
    ...Blackhole Trash folder...
    ...Thunderbird crashes...

3. The distroseries +bugs page.

Default view:

    >>> browser.open("http://localhost:9000/debian/woody/+bugs")
    >>> print browser.contents
    <!DOCTYPE...
    ...Bug Title Test...
    ...Blackhole Trash folder...

Newest:

    >>> browser.getControl(name="orderby").displayValue = ["newest"]
    >>> browser.getControl("Search").click()
    >>> print browser.contents
    <!DOCTYPE...
    ...Blackhole Trash folder...
    ...Bug Title Test...

4. The distro sourcepackage +bugs page.

Default view:

    >>> browser.open(
    ...     "http://localhost:9000/debian/+source/mozilla-firefox/+bugs")
    >>> print browser.contents
    <!DOCTYPE...
    ...Bug Title Test...
    ...Firefox does not support SVG...
    ...Blackhole Trash folder...

Sort by importance:

    >>> browser.getControl(name="orderby").displayValue = ["by importance"]
    >>> browser.getControl("Search").click()
    >>> print browser.contents
    <!DOCTYPE...
    ...Bug Title Test...
    ...Firefox does not support SVG...
    ...Blackhole Trash folder...

