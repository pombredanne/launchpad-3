
Check that the magic redirection on the Malone homepage works:

  >>> browser.open("http://launchpad.dev/malone")
  >>> browser.getControl(name='text').value = 'evolution'
  >>> browser.getControl(name="jump_to_product").click()
  >>> browser.url
  'http://launchpad.dev/products/evolution/+bugs'

