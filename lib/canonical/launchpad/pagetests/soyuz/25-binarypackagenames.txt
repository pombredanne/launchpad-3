Check if BinaryPackageName page is not broken.
This is a batched page that displays all binarypackagenames in Launchpad.

  >>> browser.open("http://localhost:9000/binarypackagenames")
  >>> browser.contents
  '...pmount...'

Check if the binarypackagename search result page is not broken.

  >>> browser.getControl(name="name").value = "firefox"
  >>> browser.getControl("Search").click()
  >>> browser.contents
  '...mozilla-firefox...'

Check if the add BinaryPackageName is not broken.

  >>> browser.addHeader("Authorization", "Basic foo.bar@canonical.com:test")
  >>> browser.open("http://localhost:9000/binarypackagenames")
  >>> browser.getLink("Add...").click()

Test if the name field validator is working

  >>> browser.getControl(name="field.name").value = "Ubuntu-LIBS2-dev"
  >>> browser.getControl("Add").click()
  >>> browser.contents
  '...An error occurred...
  ...Invalid name...'
  
Add a new valid BinaryPackageName.

  >>> browser.getControl(name="field.name").value = "ubuntu-libs2-dev"
  >>> browser.getControl("Add").click()
  >>> browser.url
  'http://localhost:9000/binarypackagenames/?name=ubuntu-libs2-dev'
  >>> browser.contents
  '...ubuntu-libs2-dev...'

