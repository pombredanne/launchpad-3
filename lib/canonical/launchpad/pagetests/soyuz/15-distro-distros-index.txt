Check if the distros root page is not broken.
In this page we can see all the Distributions in Launchpad.

  >>> browser.open("http://localhost:9000/distros")
  >>> browser.contents
  '...There are 8 distributions...'

  >>> browser.getLink("kubuntu").click()
  >>> browser.url
  'http://localhost:9000/distros/kubuntu'
  >>> browser.contents
  '...Free KDE-based Linux...'
 
