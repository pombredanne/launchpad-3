Check if the Distribution Members page is not broken.

  >>> from mechanize import LinkNotFoundError

  >>> anon_browser.open("http://localhost/distros/ubuntu")
  >>> anon_browser.getLink(
  ...     url="http://localhost/distros/ubuntu/+members").click()
  >>> anon_browser.url
  'http://localhost/distros/ubuntu/+members'

Check there's no link to change the members team without authorization,
and that going to the page anyway redirects to login.

  >>> anon_browser.handleErrors = True
  >>> try:
  ...     anon_browser.getLink("Change Members")
  ... except LinkNotFoundError:
  ...     print "LinkNotFoundError"
  LinkNotFoundError

  >>> anon_browser.open("http://localhost/distros/ubuntu/+selectmemberteam")
  >>> anon_browser.url
  'http://localhost/distros/ubuntu/+selectmemberteam/+login'

Switch to admin, and check the link to change team members appears, and works.

  >>> admin_browser.open("http://localhost/distros/ubuntu")
  >>> admin_browser.getLink("Change Members").click()
  >>> admin_browser.getControl("Members").value = 'sabdfl'
  >>> admin_browser.getControl("Change").click()
  >>> admin_browser.contents
  '...Updated on...'
  >>> admin_browser.getControl("Members").value
  'mark@hbd.com'

