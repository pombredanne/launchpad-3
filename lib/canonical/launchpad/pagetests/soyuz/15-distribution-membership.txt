Check if the Distribution Members page is not broken.

  >>> from mechanize import LinkNotFoundError

  >>> browser.open("http://localhost:9000/distros/ubuntu")
  >>> browser.getLink(url="http://localhost:9000/distros/ubuntu/+members").click()
  >>> browser.url
  'http://localhost:9000/distros/ubuntu/+members'

Check there's no link to change the members team without authorization,
and that going to the page anyway redirects to login.

  >>> browser.handleErrors = True
  >>> try:
  ...     browser.getLink("Change Members")
  ... except LinkNotFoundError:
  ...     print "LinkNotFoundError"
  LinkNotFoundError

  >>> browser.open("http://localhost:9000/distros/ubuntu/+selectmemberteam")
  >>> browser.url
  'http://localhost:9000/distros/ubuntu/+selectmemberteam/+login'

Log in.

  >>> browser.addHeader('Authorization', 'Basic foo.bar@canonical.com:test')

Check the link to change team members appears, and works.

  >>> browser.open("http://localhost:9000/distros/ubuntu")
  >>> browser.getLink("Change Members").click()
  >>> browser.getControl("Members").value = 'sabdfl'
  >>> browser.getControl("Change").click()
  >>> browser.contents
  '...Updated on...'
  >>> browser.getControl("Members").value
  'mark@hbd.com'

