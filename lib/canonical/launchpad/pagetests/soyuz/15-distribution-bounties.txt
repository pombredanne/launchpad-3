Check if the distribution bounties is not broken.

  >>> from zope.testbrowser import Browser
  >>> browser = Browser()

  >>> browser.open("http://localhost:9000/distros/ubuntu")
  >>> browser.getLink("Bounties").click()
  >>> browser.url
  'http://localhost:9000/distros/ubuntu/+bounties'
  >>> browser.contents
  '...Bounties related to Ubuntu...'

