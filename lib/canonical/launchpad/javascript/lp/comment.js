YUI().use("widget", function(Y) {

function Comment(config) {
    Comment.superclass.constructor.apply(this, arguments);
}

Comment.NAME = "Comment";

Comment.ATTRS = {};

Y.extend(Comment, Widget, {});

});
