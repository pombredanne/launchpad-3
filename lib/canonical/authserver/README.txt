Launchpad "Authserver" XML-RPC service.

This is a standalone service, not part of the Launchpad web-app.

Run it with:
    twistd -oy config.tac

There are some example .tac files in this directory.

