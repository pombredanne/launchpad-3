# This fragment was taken out of configs/default/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            # TODO: Does trebuchet actually need a root?
            # -- StuartBishop 20050706
            root /var/tmp/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch yes
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
            # Path to the log file, or '-' for stdout.
            logfile -
        </trebuchet_server>
    </trebuchet>

# This fragment was taken out of configs/dogfood/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            # TODO: Does trebuchet actually need a root?
            root /var/tmp/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch no
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
            # Path to the log file, or '-' for stdout.
            logfile -
        </trebuchet_server>
    </trebuchet>

# This fragment was taken out of configs/ftpmaster/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            logfile /srv/launchpad.ubuntu.com/staging-logs/trebuchet.log
            root /srv/launchpad.ubuntu.com/staging/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch no
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
        </trebuchet_server>
    </trebuchet>

# This fragment was taken out of configs/gina/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            logfile /home/launchpad/dists/trebuchet.log
            root /home/launchpad/dists/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch yes
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
        </trebuchet_server>
    </trebuchet>

# This fragment was taken out of configs/jubany/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            logfile /home/launchpad/dists/trebuchet.log
            root /home/launchpad/dists/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch yes
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
        </trebuchet_server>
    </trebuchet>

# This fragment was taken out of configs/librarian/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            logfile /home/launchpad/dists/trebuchet.log
            root /home/launchpad/dists/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch yes
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
        </trebuchet_server>
    </trebuchet>

# This fragment was taken out of configs/production/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            logfile /home/launchpad/dists/trebuchet.log
            root /home/launchpad/dists/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch yes
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
        </trebuchet_server>
    </trebuchet>

# This fragment was taken out of configs/production1/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            logfile /home/launchpad/dists/trebuchet.log
            root /home/launchpad/dists/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch yes
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
        </trebuchet_server>
    </trebuchet>

# This fragment was taken out of configs/production2/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            logfile /home/launchpad/dists/trebuchet.log
            root /home/launchpad/dists/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch no
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
        </trebuchet_server>
    </trebuchet>

# This fragment was taken out of configs/production3/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            logfile /home/launchpad/dists/trebuchet.log
            root /home/launchpad/dists/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch no
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
        </trebuchet_server>
    </trebuchet>

# This fragment was taken out of configs/production4/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            logfile /home/launchpad/dists/trebuchet.log
            root /home/launchpad/dists/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch no
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
        </trebuchet_server>
    </trebuchet>

# This fragment was taken out of configs/staging/launchpad.conf

    <trebuchet>
        dbuser trebuchet
        port 4280
        <trebuchet_server>
            # Configuration used if we are running a trebuchet server.
            logfile /srv/launchpad.ubuntu.com/staging-logs/trebuchet.log
            root /srv/launchpad.ubuntu.com/staging/trebuchet
            # If true, a Trebuchet server will be launched by the startup
            # script
            launch yes
            # Should Trebuchet twistd be run with --spew for debugging
            spew no
        </trebuchet_server>
    </trebuchet>
