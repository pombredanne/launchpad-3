<configure
    xmlns="http://namespaces.zope.org/zope"
    xmlns:mail="http://namespaces.zope.org/mail"
    i18n_domain="zope">

    <mail:directDelivery permission="zope.SendMail" mailer="smtp" />

    <!-- Configured outside the tree in ${ROOT}/../+mail-configure.zcml
        to avoid putting the username/password under revision control
    <mail:smtpMailer
        hostname="smtp.canonical.com" port="25"
        username="???" password="???"
        />
    -->

</configure>
