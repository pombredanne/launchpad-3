# running from buildd sequencer because someone made the slave scanner
# run every single damned minute, so this couldn't get a lock
#
#50 * * * * /home/lp_buildd/bin/queue-builder.sh
15 * * * *  cd /srv/launchpad.net/builddmaster/srcdep/ && cvs -q up

# Per https://bugs.launchpad.net/soyuz/+bug/62612
MAILTO=tom.haddon@canonical.com
# Directories older than 31 days (we want to treat the
# directories as one distinct unit)
00 00 * * * find /srv/launchpad.net/builddmaster/accepted -type d -mtime +31 -exec rm -rf {} \;
